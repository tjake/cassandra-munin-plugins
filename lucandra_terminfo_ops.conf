graph_title Lucandra.TermInfo StorageProxy operation count
graph_vlabel ops / ${graph_period}
graph_category Cassandra
graph_order reads writes

reads.label reads
reads.jmxObjectName org.apache.cassandra.db:type=ColumnFamilyStores,keyspace=Lucandra,columnfamily=TermInfo
reads.jmxAttributeName ReadCount
reads.type DERIVE
reads.min 0
reads.graph yes

writes.label writes
writes.jmxObjectName org.apache.cassandra.db:type=ColumnFamilyStores,keyspace=Lucandra,columnfamily=TermInfo
writes.jmxAttributeName WriteCount
writes.type DERIVE
writes.min 0
writes.graph yes
